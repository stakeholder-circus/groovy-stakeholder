name: codeql

on:
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CodeQL is parked for Groovy until a supported analysis path is selected."
